services:
  # Optional NVIDIA runtime for the transcoding queue. Keep this as an
  # override so CPU-only installations can continue using docker-compose.prod.yml.
  worker:
    gpus: all
    environment:
      NVIDIA_DRIVER_CAPABILITIES: compute,video,utility
